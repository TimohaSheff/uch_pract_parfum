# ОТЧЕТ О ВЫПОЛНЕНИИ ЗАДАНИЯ
## Создание интернет-магазина парфюмерии на Django

---

## 1. СОЗДАНИЕ ПРОЕКТА И УСТАНОВКА БИБЛИОТЕК

### 1.1. Инициализация Django проекта

Создан проект `perfume_shop` с использованием Django framework. Структура проекта:

```
проект/
├── manage.py
├── requirements.txt
├── perfume_shop/
│   ├── __init__.py
│   ├── settings.py
│   ├── urls.py
│   ├── wsgi.py
│   └── asgi.py
├── shop/
│   ├── views.py
│   ├── urls.py
│   ├── models.py
│   └── templates/
│       └── shop/
│           ├── base.html
│           ├── index.html
│           ├── catalog.html
│           └── contacts.html
└── static/
    └── shop/
        ├── css/
        │   └── style.css
        └── js/
            └── main.js
```

### 1.2. Файл зависимостей (requirements.txt)

```txt
Django==4.2.7
Pillow==10.1.0
```

### 1.3. Основные настройки проекта (perfume_shop/settings.py)

**Ключевые настройки:**
- `ROOT_URLCONF = 'perfume_shop.urls'` - главный файл маршрутизации
- `INSTALLED_APPS` включает приложение `shop`
- Настроены пути для статических файлов и медиа
- Язык интерфейса: русский (`LANGUAGE_CODE = 'ru-ru'`)

---

## 2. СОЗДАНИЕ СТРАНИЦ САЙТА

### 2.1. Главная страница (shop/templates/shop/index.html)

**Реализовано:**
- Заголовок с девизом сайта: "Ваш аромат - ваша индивидуальность"
- Подзаголовок: "Откройте для себя мир изысканных ароматов"
- Слайдер с тремя слайдами:
  1. "Новая коллекция" - эксклюзивные ароматы
  2. "Премиум качество" - оригинальная продукция
  3. "Доставка по всей стране" - быстрая доставка
- Секция с преимуществами (быстрая доставка, гарантия качества, поддержка 24/7)

### 2.2. Страница каталога (shop/templates/shop/catalog.html)

**Реализовано:**
- Заголовок страницы "Каталог парфюмерии"
- Фильтры по брендам и типам парфюмерии
- Сетка товаров с карточками продукции
- Каждая карточка содержит: изображение, название, бренд, цену, кнопку "В корзину"

### 2.3. Страница контактов (shop/templates/shop/contacts.html)

**Реализовано:**
- Информация о контактах:
  - Адрес магазина
  - Телефон
  - Email
  - Режим работы
- Форма обратной связи для связи с администрацией

### 2.4. Представления (shop/views.py)

```python
from django.shortcuts import render

def index(request):
    """Главная страница"""
    return render(request, 'shop/index.html')

def catalog(request):
    """Страница каталога"""
    return render(request, 'shop/catalog.html')

def contacts(request):
    """Страница контактов"""
    return render(request, 'shop/contacts.html')
```

### 2.5. Маршрутизация (shop/urls.py)

```python
from django.urls import path
from . import views

urlpatterns = [
    path('', views.index, name='index'),
    path('catalog/', views.catalog, name='catalog'),
    path('contacts/', views.contacts, name='contacts'),
]
```

---

## 3. СОЗДАНИЕ МЕНЮ НАВИГАЦИИ

### 3.1. Базовый шаблон (shop/templates/shop/base.html)

**Реализованные элементы навигации:**

1. **Логотип и название сайта**
   - Иконка парфюма (Font Awesome)
   - Название: "ПарфюмЭлит"
   - Оба элемента являются ссылкой на главную страницу (`{% url 'index' %}`)

2. **Меню навигации**
   - Ссылка "Главная" → `/`
   - Ссылка "Каталог" → `/catalog/`
   - Ссылка "Контакты" → `/contacts/`

3. **Кнопки авторизации**
   - Для неавторизованных пользователей:
     - Кнопка "Вход" → `/admin/login/`
     - Кнопка "Регистрация" → `/admin/`
   - Для авторизованных пользователей:
     - Отображение имени пользователя
     - Кнопка "Выход" → `/admin/logout/`

**Код навигации:**
```html
<nav class="navbar">
    <div class="container">
        <div class="nav-content">
            <a href="{% url 'index' %}" class="logo-section">
                <div class="logo">
                    <i class="fas fa-spray-can"></i>
                </div>
                <span class="site-name">ПарфюмЭлит</span>
            </a>
            <ul class="nav-menu">
                <li><a href="{% url 'index' %}">Главная</a></li>
                <li><a href="{% url 'catalog' %}">Каталог</a></li>
                <li><a href="{% url 'contacts' %}">Контакты</a></li>
            </ul>
            <div class="auth-buttons">
                {% if user.is_authenticated %}
                    <a href="#" class="btn btn-secondary">{{ user.username }}</a>
                    <a href="{% url 'admin:logout' %}" class="btn btn-primary">Выход</a>
                {% else %}
                    <a href="{% url 'admin:login' %}" class="btn btn-secondary">Вход</a>
                    <a href="{% url 'admin:index' %}" class="btn btn-primary">Регистрация</a>
                {% endif %}
            </div>
        </div>
    </div>
</nav>
```

---

## 4. РЕАЛИЗАЦИЯ СЛАЙДЕРА НА ГЛАВНОЙ СТРАНИЦЕ

### 4.1. HTML структура слайдера

Слайдер содержит три слайда с уникальным контентом:
- Каждый слайд имеет градиентный фон
- Текстовая информация о преимуществах
- Анимация перехода между слайдами

### 4.2. JavaScript функциональность (static/shop/js/main.js)

**Основные функции:**
- Автоматическая смена слайдов каждые 5 секунд
- Ручное управление через кнопки "Назад"/"Вперед"
- Навигация через точки (dots)
- Пауза при наведении мыши

**Ключевой код:**
```javascript
let currentSlideIndex = 0;
const slides = document.querySelectorAll('.slide');
const dots = document.querySelectorAll('.dot');

function showSlide(index) {
    slides.forEach(slide => slide.classList.remove('active'));
    dots.forEach(dot => dot.classList.remove('active'));
    
    if (index >= slides.length) {
        currentSlideIndex = 0;
    } else if (index < 0) {
        currentSlideIndex = slides.length - 1;
    } else {
        currentSlideIndex = index;
    }
    
    slides[currentSlideIndex].classList.add('active');
    dots[currentSlideIndex].classList.add('active');
}

// Автоматическая смена слайдов
let slideInterval = setInterval(() => {
    changeSlide(1);
}, 5000);
```

### 4.3. CSS стилизация (static/shop/css/style.css)

**Особенности дизайна:**
- Градиентные фоны для каждого слайда
- Плавные анимации переходов
- Адаптивный дизайн для мобильных устройств
- Современный UI с использованием градиентов и теней

---

## 5. СТИЛИЗАЦИЯ И ДИЗАЙН

### 5.1. Цветовая схема
- Основной цвет: градиент от #667eea до #764ba2
- Белый текст на цветном фоне
- Тени и эффекты для глубины

### 5.2. Адаптивность
- Мобильная версия с вертикальным меню
- Адаптивная сетка для каталога
- Оптимизация для различных размеров экранов

---

## 6. ЗАГРУЗКА ПРОЕКТА НА GIT

### 6.1. Инициализация репозитория

```bash
git init
```

### 6.2. Создание .gitignore

Исключены из репозитория:
- `__pycache__/` и `*.pyc` файлы
- `db.sqlite3` (база данных)
- `venv/` (виртуальное окружение)
- `.env` файлы
- IDE настройки

### 6.3. Коммиты проекта

Выполнены следующие коммиты:
1. "Создание структуры Django проекта и requirements.txt"
2. "Создание приложения shop с базовыми настройками"
3. "Создание базового шаблона с навигацией"
4. "Создание страниц: Главная, Каталог, Контакты"
5. "Добавление CSS стилей и JavaScript для слайдера"

### 6.4. Загрузка на удаленный репозиторий

```bash
git remote add origin <URL_РЕПОЗИТОРИЯ>
git branch -M main
git push -u origin main
```

---

## 7. РЕЗУЛЬТАТЫ ВЫПОЛНЕНИЯ

### ✅ Выполненные задачи:

1. ✅ Создан Django проект с необходимыми библиотеками
2. ✅ Созданы три страницы: Главная, Каталог, Контакты
3. ✅ Реализовано меню навигации с:
   - Логотипом и названием сайта (переход на главную)
   - Ссылками на все страницы
   - Кнопками авторизации и регистрации
4. ✅ На главной странице добавлены:
   - Заголовок с девизом сайта
   - Слайдер с тремя слайдами
5. ✅ Проект загружен на Git с коммитами

### 📋 Технические детали:

- **Framework:** Django 4.2.7
- **Язык программирования:** Python 3.11+
- **Frontend:** HTML5, CSS3, JavaScript
- **Иконки:** Font Awesome 6.4.0
- **База данных:** SQLite (для разработки)

---

## 8. ИНСТРУКЦИЯ ПО ЗАПУСКУ

### 8.1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 8.2. Применение миграций

```bash
python manage.py migrate
```

### 8.3. Создание суперпользователя

```bash
python manage.py createsuperuser
```

### 8.4. Запуск сервера разработки

```bash
python manage.py runserver
```

Сайт будет доступен по адресу: `http://127.0.0.1:8000/`

---

## ЗАКЛЮЧЕНИЕ

Все задачи задания успешно выполнены. Создан полнофункциональный интернет-магазин парфюмерии на Django с современным дизайном, навигацией, слайдером и всеми необходимыми страницами. Проект готов к дальнейшей разработке и может быть развернут на продакшн-сервере.

